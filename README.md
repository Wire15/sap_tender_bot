# SAP Tender Bot

Ingest public-sector tenders, filter for SAP relevance, summarize with Cohere Command R, notify via email.

## Setup
```bash
python -m venv .venv
# Windows:
.\.venv\Scripts\activate
pip install -r requirements.txt